# newton-machine-macros

Implementation detail for [`newton-machine`](https://crates.io/crates/newton-machine).

Do not depend on this crate directly:

```toml
newton-machine = { version = "0.2", features = ["macros"] }
```

See `newton-machine` docs: `#[derive(Topology)]`, `#[derive(IntoNode)]`, `#[machine]`.
