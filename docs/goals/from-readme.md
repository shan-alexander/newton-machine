---
tags: [goal, readme, generated]
node_type: goal
aliases: [from-readme, newton-machine]
generated: true
source: README.md
---
# Goals harvested from README

> Generated by `rustbrain bootstrap`. Edit freely; re-run with `--force` to regenerate.

Project title: **newton-machine**

## Overview

**Newtonian state machines** for Rust: Harel configuration as nested ADTs, the Elm Architecture as the only mutation protocol, and history as an inertial sidecar.

A Newton machine is not a new automaton. Harel already defined the kinematics (XOR/AND, hierarchy, history, run-to-completion). This crate names the **dynamics and conservation laws** for embedding those kinematics in a running Rust system: Unidirectional Configuration Architecture (UCA).

> A Newton machine may lock itself. Only the gateway may lock the wire. Only the venue can save you when both are dead.

**Status:** `0.0.0` is the design crate. The public types encode the family laws. The compiled RTC/LCA engine is not claimed yet. Nothing here is stable.

## Why this family exists

Three traditions want three different things:

| Tradition | Superpower | Cost if you take it as the whole design |
| --- | --- | --- |
| Elm TEA | One model, one `Msg`, pure `update` | No hierarchy or orthogonality of its own |
| Harel statecharts | Hierarchy, orthogonal regions, history, RTC | Interpreters and string ids are slow and un-Rusty |
| Rust typestate | Illegal transitions do not compile | Orthogonal × deep history explodes the type space |

A Newton machine keeps typestate at the edges, Harel configuration as **data** in the middle, and TEA as the **only door**.


